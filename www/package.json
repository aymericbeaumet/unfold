{
  "name": "retromap",
  "version": "1.0.0",
  "scripts": {
    "start": "API_URL=http://localhost:8080 parcel serve index.html",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "ol": "^6.14.1"
  },
  "devDependencies": {
    "parcel": "^2.5.0"
  }
}
